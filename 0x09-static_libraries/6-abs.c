#include "main.h"

/**
 * _abs - absolute value of an integer to be computes
 *
 * @n: check int
 *
 * Return: absolute int value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
