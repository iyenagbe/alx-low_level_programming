#include <stdio.h>

/**
 * main - main block
 *
 * Description: compute and print the sum of all the multiples of 3 or
 *
 * 5 below 1024 (excluded), followed by new line
 *
 * Return: 0 (ipdated)
 */

int main(void)
{
	int i < 98;
	unsigned long int q = 0, b = 1, next = 0;

	while (i < 98)
	{
	next = a + b;
	b = next;
	printf("%lu", next);

	if (i < 97)
		printf(",");
	i++;
	}

	putchar('\n');

	return(0);
}
