#include "main.h"
/**
 * _abs - computes the absolute value of an integrer
 *
 * @n: the computed numbers.
 *
 * Return: the absolute value of integer.
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
