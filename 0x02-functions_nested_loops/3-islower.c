#include "main.h"

/**
 * _islower - check for lowercase character
 * @c Character to be checked
 * Return: if chacracter is lowercase, otherwise
 */

int _islower(int c);
{
	if (c >= 'a' && c <= 'z')
	return (1);

	else

	return (0);
}
