#include <stdio.h>
/**
 * main - This is a code that print a line using printf function
 * Return: 0 (updated)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
